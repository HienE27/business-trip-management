feat(workflow): standardize monthly schedule workflow M02-M05

Backend:
- Add POST /api/v1/schedules/bulk endpoint for bulk-creating any shift type (L01-L04)
  with partial-success response (per-entry success/failure)
- Add GET /api/v1/periods/{id}/publish/dry-run endpoint that validates a
  period without persisting (runs conflict check + staffing coverage)
- Expose ConflictDetectionService.validateStaffingCoverage via new endpoint
- New DTOs: BulkScheduleRequest, BulkScheduleResponse, PublishDryRunResponse

Frontend:
- Add BulkScheduleModal component with two phases:
  Phase 1: date-range picker + per-date staff assignment table
  Phase 2: dual-stat success/failure tiles + scrollable result list
- Add BulkDatePickerModal for selecting multiple dates before bulk assign
- Wire into ScheduleByTypePage: "Gán hàng loạt" button (DRAFT periods only)
- Integrate dry-run publish into conflict-check step: show canPublish badge
  when no conflicts, show conflict table + coverage gaps when there are issues
- Add bulkCreateSchedules + dryRunPublish to API client
- SectionCard description prop widened to ReactNode
