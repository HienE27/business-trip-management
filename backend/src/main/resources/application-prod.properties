# Production must use explicit schema migrations and must never fall back to a
# development JWT key. SecurityValidator fails startup when JWT_SECRET is absent
# or unsafe.
spring.jpa.hibernate.ddl-auto=validate
jwt.secret=${JWT_SECRET:}
